# Local Source Files

Place private workbook and email source files here for local analysis.

These files can contain student or lead names, emails, and phone numbers, so the directory contents are ignored by Git. The deployed app should use live HubSpot data through Streamlit secrets plus the sanitized `public_data/baseline/` files committed to the repository.
