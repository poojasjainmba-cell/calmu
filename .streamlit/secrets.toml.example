HUBSPOT_ACCESS_TOKEN = "pat-na1-REPLACE_ME"

# Optional. Comma-separated exact HubSpot contact property internal names.
# Use this only when CalMU has confirmed custom fields in HubSpot.
HUBSPOT_CONTACT_PROPERTIES = ""

# Optional. Start with 1000 for a fast hosted smoke test; raise after deployment is stable.
HUBSPOT_MAX_CONTACTS = "1000"

# Optional. Protects the Raw Audit Data page when set.
RAW_AUDIT_PASSWORD = "replace-with-a-local-password"
